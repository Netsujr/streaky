<div id="habit-form-modal" class="hidden fixed inset-0 bg-black/60 backdrop-blur-sm z-50 flex items-center justify-center p-4">
  <div class="bg-white/95 backdrop-blur-md rounded-2xl shadow-2xl p-8 max-w-md w-full border border-gray-200/50 transform transition-all">
    <%= turbo_frame_tag "habit_form" do %>
      <%= render "habits/form", habit: nil %>
    <% end %>
  </div>
</div>
