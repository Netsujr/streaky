<div class="max-w-md mx-auto">
  <h1 class="text-2xl font-bold mb-6">New Habit</h1>

  <%= turbo_frame_tag "habit_form" do %>
    <%= render "habits/form", habit: @habit %>
  <% end %>
</div>

<script>
  // If we're on the new page (not in modal), redirect to dashboard with modal
  if (!document.getElementById('habit-form-modal')) {
    window.location.href = '/';
  }
</script>
