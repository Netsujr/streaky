<!DOCTYPE html>
<html>
  <head>
    <title>Streaky - Habit Tracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 min-h-screen" data-controller="flash-toast">
    <div id="flash" class="fixed top-4 left-4 right-4 sm:left-auto sm:right-4 z-50 space-y-2 max-w-md sm:max-w-md mx-auto sm:mx-0">
      <% flash.each do |type, message| %>
        <%= render "layouts/flash_toast", message: message, type: type %>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
